# 🎭 퉁퉁이 AI 캐릭터 만들기 - 전체 파이프라인

처음부터 끝까지 퉁퉁이 AI 캐릭터를 만드는 완전한 가이드입니다.

---

## 📊 전체 흐름도

```
┌─────────────────────────────────────────────────────────┐
│ 1단계: 데이터 수집 (유튜브)                              │
│    - 뽀로로 유튜브 영상에서 퉁퉁이 음성 다운로드         │
│    - 필요량: 5-10분 분량                                │
│    - 도구: yt-dlp                                       │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 2단계: 음성 분리 (배경음악 제거)                         │
│    - 배경음악/효과음 제거                                │
│    - 순수 목소리만 추출                                  │
│    - 도구: Spleeter/Demucs                              │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 3단계: 음성 전처리                                       │
│    - 노이즈 제거                                         │
│    - 볼륨 정규화                                         │
│    - 3-10초 세그먼트로 분할                              │
│    - 무음 구간 제거                                      │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 4단계: 자막/대본 생성 (선택)                             │
│    - Whisper로 음성→텍스트 변환                          │
│    - 각 세그먼트에 대본 매칭                             │
│    - GPT-SoVITS 학습용 메타데이터 생성                   │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 5단계: GPT-SoVITS 모델 학습                              │
│    - 퉁퉁이 목소리 학습 (1-3시간)                        │
│    - 체크포인트 저장                                     │
│    - 품질 테스트                                         │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 6단계: 챗봇 통합                                         │
│    - Gemini 2.0 Flash-Lite (대화 생성)                  │
│    - 학습된 TTS 모델 (음성 합성)                         │
│    - 완성!                                               │
└─────────────────────────────────────────────────────────┘
```

---

## 🎬 1단계: 유튜브에서 퉁퉁이 음성 수집

### A. 유튜브 영상 찾기

**추천 영상:**
- "뽀로로 퉁퉁이 모음"
- "뽀로로 시즌1 퉁퉁이"
- 퉁퉁이가 많이 나오는 에피소드

**필요한 분량:**
- 최소: 5분 (테스트용)
- 권장: 10-15분 (고품질)
- 최대: 30분 (최고 품질)

### B. 음성 다운로드 (자동)

#### 방법 1: 기존 파이프라인 사용 (권장)

**1. 설정 파일 작성**
`configs/character_config.yaml`에 퉁퉁이 추가:

```yaml
characters:
  tongtong:
    name: "퉁퉁이"
    description: "힘이 세고 순수한 곰"

    youtube_urls:
      - "https://www.youtube.com/watch?v=실제유튜브영상ID1"
      - "https://www.youtube.com/watch?v=실제유튜브영상ID2"
      - "https://www.youtube.com/watch?v=실제유튜브영상ID3"

    personality:
      traits:
        - "순수함"
        - "힘이 셈"
        - "엄마를 그리워함"
        - "느리지만 착함"
      speech_style: "느리고 굵은 목소리, 단순한 말투"
      age_group: "5-7세"

    training:
      target_duration: 600  # 10분
      min_segment_length: 3
      max_segment_length: 10
      sample_rate: 22050

      gpt_sovits:
        epochs: 100
        batch_size: 4
        learning_rate: 0.0001
        save_interval: 10

    audio_processing:
      noise_reduction: true
      normalization: true
      trim_silence: true
      target_loudness: -18  # 굵은 목소리
```

**2. 다운로드 실행**

```bash
# 전체 파이프라인 실행 (데이터 수집 + 전처리 + 학습)
python scripts/train_multiple_characters.py --character tongtong

# 또는 다운로드만 실행
python -c "
from tools.youtube_downloader import YouTubeAudioDownloader

downloader = YouTubeAudioDownloader(output_dir='./data/raw')

urls = [
    'https://www.youtube.com/watch?v=영상ID1',
    'https://www.youtube.com/watch?v=영상ID2',
]

for url in urls:
    downloader.download_audio(url, character_name='tongtong')
"
```

#### 방법 2: 수동 다운로드

```bash
# yt-dlp로 직접 다운로드
yt-dlp -f "bestaudio" \
  -o "data/raw/tongtong_%(title)s.%(ext)s" \
  "https://www.youtube.com/watch?v=영상ID"

# MP3로 변환
yt-dlp -f "bestaudio" \
  --extract-audio \
  --audio-format mp3 \
  --audio-quality 0 \
  -o "data/raw/tongtong_%(title)s.%(ext)s" \
  "https://www.youtube.com/watch?v=영상ID"
```

### C. 다운로드 결과 확인

```bash
# 다운로드된 파일 확인
ls data/raw/tongtong*

# 예상 출력:
# data/raw/tongtong_video1.mp3
# data/raw/tongtong_video2.mp3
```

---

## 🎵 2단계: 배경음악 제거 (보컬 분리)

**왜 필요한가?**
- 뽀로로 영상에는 배경음악이 있음
- 순수한 목소리만 추출해야 학습 품질 향상

### 자동 실행

```python
from tools.vocal_separator import VocalSeparator

separator = VocalSeparator(method="spleeter")

# 배경음 제거
vocals_path = separator.separate(
    input_path="data/raw/tongtong_video1.mp3",
    output_dir="data/vocals/tongtong",
    stems="2stems"  # vocals + accompaniment
)

print(f"보컬 파일: {vocals_path}")
# 출력: data/vocals/tongtong/tongtong_video1/vocals.wav
```

### 결과 확인

```bash
# 보컬 파일 확인
ls data/vocals/tongtong/

# 예상 구조:
# data/vocals/tongtong/
#   ├── video1/
#   │   ├── vocals.wav      ← 퉁퉁이 목소리만
#   │   └── accompaniment.wav  ← 배경음악
#   └── video2/
#       ├── vocals.wav
#       └── accompaniment.wav
```

---

## 🔧 3단계: 음성 전처리

**목표:**
- 노이즈 제거
- 볼륨 일정하게 (정규화)
- 3-10초 구간으로 자르기
- 무음 구간 제거

### 자동 실행

```python
from tools.audio_preprocessor import AudioPreprocessor

preprocessor = AudioPreprocessor(sample_rate=22050)

# 전처리 실행
output_files = preprocessor.process_audio(
    input_path="data/vocals/tongtong/video1/vocals.wav",
    output_dir="data/processed/tongtong",
    character_name="tongtong",
    enable_noise_reduction=True,
    enable_normalization=True,
    segment_config={
        'min_length': 3.0,   # 최소 3초
        'max_length': 10.0,  # 최대 10초
        'overlap': 0.5       # 0.5초 겹침
    }
)

print(f"생성된 세그먼트 수: {len(output_files)}")
# 출력: 생성된 세그먼트 수: 45
```

### 결과 확인

```bash
# 전처리된 파일 확인
ls data/processed/tongtong/

# 예상 구조:
# data/processed/tongtong/
#   ├── segment_001.wav
#   ├── segment_002.wav
#   ├── segment_003.wav
#   ...
#   └── segment_045.wav
```

---

## 📝 4단계: 대본 생성 (선택사항)

**왜 필요한가?**
- GPT-SoVITS는 텍스트가 있으면 품질이 더 좋음
- 각 음성 세그먼트에 대응하는 대사 필요

### Whisper로 자동 생성

```python
from tools.speech_to_text import create_stt
import os

stt = create_stt(method="whisper", model_size="medium")

# 모든 세그먼트에 대해 텍스트 생성
segment_dir = "data/processed/tongtong"
transcript_file = "data/datasets/tongtong/transcript.txt"

with open(transcript_file, 'w', encoding='utf-8') as f:
    for audio_file in sorted(os.listdir(segment_dir)):
        if audio_file.endswith('.wav'):
            audio_path = os.path.join(segment_dir, audio_file)

            # 음성→텍스트
            result = stt.transcribe_file(audio_path, language="ko")
            text = result["text"].strip()

            # 파일명|텍스트 형식으로 저장
            f.write(f"{audio_file}|{text}\n")
            print(f"{audio_file}: {text}")

print(f"대본 저장: {transcript_file}")
```

### 결과 예시

```
# data/datasets/tongtong/transcript.txt

segment_001.wav|나는 퉁퉁이야
segment_002.wav|너희들 뭐하니?
segment_003.wav|나도 같이 놀고 싶어
segment_004.wav|엄마... 보고 싶어
...
```

---

## 🤖 5단계: GPT-SoVITS 모델 학습

**가장 중요한 단계!** 퉁퉁이 목소리를 학습합니다.

### A. 학습 데이터셋 준비

```bash
# 디렉토리 구조 확인
data/datasets/tongtong/
  ├── wavs/              # 음성 파일들
  │   ├── segment_001.wav
  │   ├── segment_002.wav
  │   └── ...
  └── transcript.txt     # 대본 파일
```

### B. 학습 실행

#### 방법 1: 전체 파이프라인 (권장)

```bash
# configs/character_config.yaml에 퉁퉁이 설정 추가 후
python scripts/train_multiple_characters.py --character tongtong
```

#### 방법 2: GPT-SoVITS만 실행

```bash
# GPT-SoVITS 저장소로 이동
cd GPT-SoVITS

# 학습 실행
python train.py \
  --data_dir ../data/datasets/tongtong \
  --output_dir ../models/gpt_sovits/tongtong \
  --epochs 100 \
  --batch_size 4
```

### C. 학습 진행 상황 모니터링

```bash
# 로그 확인
tail -f logs/training.log

# 예상 출력:
# Epoch [1/100] Step [10/200] Loss: 0.523
# Epoch [1/100] Step [20/200] Loss: 0.412
# ...
# Epoch [100/100] Completed! Model saved.
```

### D. 학습 시간

| 데이터 양 | GPU | CPU |
|-----------|-----|-----|
| 5분 | 30분 | 3시간 |
| 10분 | 1시간 | 6시간 |
| 30분 | 3시간 | 12시간+ |

---

## 🎤 6단계: 모델 테스트

학습이 끝나면 테스트해봅니다!

### A. 간단한 테스트

```python
from tools.text_to_speech import CharacterTTS

# 퉁퉁이 TTS 로드
tts = CharacterTTS(character_name="tongtong")

# 음성 생성
output = tts.synthesize(
    text="안녕? 나는 퉁퉁이야!",
    output_path="output/tongtong_test.wav"
)

# 재생
tts.play_audio(output)
```

### B. 다양한 문장 테스트

```python
test_sentences = [
    "너희들 뭐하니?",
    "나도 같이 놀고 싶어!",
    "엄마... 보고 싶어",
    "내가 제일 힘이 세!",
    "배고파... 밥 먹고 싶어"
]

for i, text in enumerate(test_sentences):
    output = tts.synthesize(
        text=text,
        output_path=f"output/tongtong_test_{i+1}.wav"
    )
    print(f"생성됨: {output}")
```

---

## 🎭 7단계: 챗봇 통합

이제 Gemini + TTS를 결합합니다!

```python
from tools.chatbot import create_chatbot
from tools.text_to_speech import CharacterTTS

# 퉁퉁이 챗봇
chatbot = create_chatbot(
    character_name="퉁퉁이",
    personality_traits=[
        "순수함",
        "힘이 셈",
        "엄마를 그리워함",
        "느리지만 착함"
    ],
    speech_style="느리고 굵은 목소리, 단순한 문장"
)

# 퉁퉁이 TTS
tts = CharacterTTS(character_name="tongtong")

# 대화 테스트
user_input = "퉁퉁아, 오늘 뭐하고 놀까?"
response = chatbot.get_response(user_input)
print(f"퉁퉁이: {response}")

# 음성으로 재생
tts.synthesize(response, "output/response.wav")
tts.play_audio("output/response.wav")
```

---

## 📊 체크리스트

### ✅ 1단계: 데이터 수집
- [ ] 유튜브 영상 URL 찾기 (5-10개)
- [ ] 음성 다운로드 (5-30분 분량)
- [ ] 파일 확인: `data/raw/tongtong_*.mp3`

### ✅ 2단계: 보컬 분리
- [ ] Spleeter/Demucs 설치
- [ ] 배경음 제거 실행
- [ ] 파일 확인: `data/vocals/tongtong/*/vocals.wav`

### ✅ 3단계: 전처리
- [ ] 노이즈 제거
- [ ] 세그먼트 분할 (3-10초)
- [ ] 파일 확인: `data/processed/tongtong/segment_*.wav`

### ✅ 4단계: 대본 생성 (선택)
- [ ] Whisper 설치
- [ ] 자동 대본 생성
- [ ] 파일 확인: `data/datasets/tongtong/transcript.txt`

### ✅ 5단계: 모델 학습
- [ ] GPT-SoVITS 설정
- [ ] 학습 실행 (1-3시간)
- [ ] 체크포인트 확인: `models/gpt_sovits/tongtong/`

### ✅ 6단계: 테스트
- [ ] 간단한 문장 생성
- [ ] 품질 확인
- [ ] 필요 시 재학습

### ✅ 7단계: 챗봇 통합
- [ ] Gemini 챗봇 연결
- [ ] TTS 통합
- [ ] 완성!

---

## 🚀 빠른 시작 (올인원)

전체 파이프라인을 한 번에 실행:

```bash
# 1. character_config.yaml에 퉁퉁이 추가

# 2. 전체 실행
python scripts/train_multiple_characters.py --character tongtong

# 이 명령어가 자동으로:
# - 유튜브 다운로드
# - 보컬 분리
# - 전처리
# - 대본 생성
# - 모델 학습
# 모두 실행합니다!
```

---

## ⏱️ 예상 소요 시간

| 단계 | 시간 (GPU) | 시간 (CPU) |
|------|-----------|-----------|
| 1. 데이터 수집 | 10분 | 10분 |
| 2. 보컬 분리 | 5분 | 15분 |
| 3. 전처리 | 5분 | 10분 |
| 4. 대본 생성 | 10분 | 30분 |
| 5. 모델 학습 | 1시간 | 6시간 |
| **총 시간** | **~1.5시간** | **~7시간** |

---

## 💡 팁

1. **처음에는 데이터 적게** 시작 (5분만)
2. **품질 확인 후** 데이터 추가
3. **GPU 사용** 권장 (학습 속도 10배)
4. **체크포인트 저장** 자주 하기
5. **여러 영상** 섞으면 품질 향상

---

## ❓ 자주 묻는 질문

### Q1: 유튜브 영상 저작권 문제는?
→ 개인 연구/테스트용이면 OK. 상업적 사용은 주의.

### Q2: 데이터가 5분밖에 없으면?
→ 테스트는 가능. 고품질은 10분+ 권장.

### Q3: 학습 중 중단하면?
→ 체크포인트에서 재개 가능.

### Q4: GPU 없으면 안 되나?
→ CPU도 가능. 단, 시간 오래 걸림.

---

**다음 단계: 실제로 퉁퉁이 설정 파일 만들기!**
